<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.server.mapper.UserMapper">
    <select id="selectOneUser" parameterType="com.server.model.UserDTO" resultType="com.server.model.UserDTO">
        SELECT
            id AS id,
            username AS strUserName,
            email AS strEmail,
            password AS strPassword,
            role AS strRole,
            phone_number AS strPhoneNumber
        FROM member
        WHERE 1 = 1
         AND email = #{strEmail}
         AND email = #{strPassword}
    </select>
</mapper>